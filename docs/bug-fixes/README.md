# Bug Fix Records

This folder stores short, structured bug records for resolved bugs and bugs still in progress.

Use one Markdown file per bug so the history for each issue stays easy to scan.

If a bug is being fixed over multiple iterations, update the existing file instead of creating duplicate records.

Keep each file concise and practical. Focus on the problem, what was learned, what changed, and how it was verified.
